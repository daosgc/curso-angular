import { TransformarTxtPipe } from './transformar-txt.pipe';

describe('TransformarTxtPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformarTxtPipe();
    expect(pipe).toBeTruthy();
  });
});
